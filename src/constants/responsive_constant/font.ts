import GlobalConstant from "../global_constant";

const fontSizeUnit = GlobalConstant.DeviceHeight / 74;

const bigger = fontSizeUnit * 1.7;
const big = fontSizeUnit * 1.39;
const medium = fontSizeUnit * 0.9;
const small = fontSizeUnit * 0.8;
const smaller = fontSizeUnit * 0.7;

export const FontSize = {
  bigger: bigger,
  big: big,
  medium: medium,
  small: small,
  smaller: smaller,
};
